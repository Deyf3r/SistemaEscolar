@model IEnumerable<Sistema_Escolar.Data.Entities.Estudiantes>

@{
    ViewData["Title"] = "Index";
}

<h1>Lista Estudiantes</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.EstudiantesId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Nombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Apellido)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.EstudiantesId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Apellido)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.EstudiantesId }) |
                @Html.ActionLink("Details", "Details", new {  id=item.EstudiantesId }) |
            
            </td>
        </tr>
}
    </tbody>
</table>
